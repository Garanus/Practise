{% extends 'base.html.twig' %}

{% block title %}Zalogowano!{% endblock %}

{% block body %}
    <h1>Logged IN</h1>
<form action="{{ path('logout') }}">
    <input type="hidden" name="_target_path" value="{{ path('logout') }}"/>
    <div class="center" style="padding-bottom: 50px;padding-top: 30px;">
        <button type="submit" class="btn btn-info">Wyloguj</button>
    </div>
</form>
{% endblock %}
